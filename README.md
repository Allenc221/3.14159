# 3.14159

Do it good

## 快速查询天气

此仓库包含 `weather.py`，用于快速查询指定城市的天气。使用前需要在环境变量中设置 `OPENWEATHER_API_KEY`，可在 [OpenWeather](https://openweathermap.org/) 注册获取。

### 使用方法

```bash
export OPENWEATHER_API_KEY=你的API密钥
python weather.py 北京
```

输出示例：
```
北京: 晴, 25°C
```
